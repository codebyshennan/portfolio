# Local Notes

This folder was created as a placeholder for the missing `codebyshennan` repo.

If the actual project is later copied or cloned here:

- inspect the page/component that renders the profile body
- verify whether it outputs raw Markdown strings
- if so, switch to a Markdown renderer with GFM support
- preserve any existing content source format before changing data files
